# Current repository YOLOv8-Seg dataset.
# This file matches the data that is actually present today: one class.
# Component-aware annotations are specified in dataset/components.yaml.

path: ./dataset
train: images/train
val: images/val
test: images/test

nc: 1
names:
  0: eyewear
